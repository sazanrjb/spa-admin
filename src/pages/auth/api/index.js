import { API_HOST } from "@/configs";

export const ME = `${API_HOST}/api/user/me`;

export const LOGIN = `${API_HOST}/api/token`;

export const RESET_PASSWORD = `${API_HOST}/api/user/forgotpassword`;